import { useState } from 'react';
import { StyleSheet } from 'react-native'; // Importa TouchableOpacity

const ConfigStylo = () => {
    const colors = {
        color1: '#007AFF',
        colorHover: 'rgba(107, 147, 255, 0.11)',
        color2: 'rgb(255, 107, 107)',
    }

    return { colors }
}

export default ConfigStylo